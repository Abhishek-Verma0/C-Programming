//  we are going to merging two array it does not matters whether they have something comman or not we can use a third array or we can do this into any one of the array just by extending array  if array is not statically defined
#include<stdio.h>
int main(){

}