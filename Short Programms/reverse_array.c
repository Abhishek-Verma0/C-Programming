#include<stdio.h>
int main(){
    int i,num;

    int arr[num];
        //***************************** this block of code is  only for reversal of array*****************
    //   for (i = 0; i < num / 2; i++) {                             
    //     int temp = arr[i];
    //     arr[i] = arr[num - i - 1];
    //     arr[num - i - 1] = temp;
    // }
    
}